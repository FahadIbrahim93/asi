# COOM 1.0.1 environment dependencies, frozen for the qualification-only runtime.
# `opencv-python-headless` replaces upstream `opencv-python` because this smoke never renders.
vizdoom==1.3.0
opencv-python-headless==4.11.0.86
scipy==1.11.4
gymnasium==0.28.1
numpy==1.26.4
